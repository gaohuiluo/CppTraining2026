# 模块生成风格规范（所有模块必须遵守）

学习者背景：扎实的 C 语言基础；C++ 面向对象只有概念性认知（看过书、工作没用过）。目标是系统学 C++，最终转音视频，但当前阶段要**通用、完整、标准**的 C++ 基础，不向音视频倾斜。

统一约束：
- 目标标准 **C++17**，编译器 **Visual Studio / MSVC**。
- 节奏「业余慢跑」，风格「够用就上手」（偏实践，原理讲到够用即止，但基础要全）。
- 全程突出 **vs C 的对比**（便于借助 C 基础记忆）。
- 回复/文档用**中文**；`.cpp` 文件名和文件夹名用 **ASCII**（方便 `cl` 命令行）。

每个模块目录结构（与 M1/M2 完全一致）：
```
Mn_<name>/
├── principles.md    原理精讲
├── exercises.md     练习（难度分级 ⭐/⭐⭐/⭐⭐⭐，通常 8 题 + 1 个 mini 项目）
└── answers/         每题一个参考实现（.cpp），逐行/关键行中文注释
    └── mini/        mini 项目的实现
```

principles.md 必备板块（照抄 M1/M2 的结构与语气）：
1. 「一句话总览」开头。
2. 每个重要概念配一段 **vs C 对比**（有对比表更好）。
3. 关键概念给最小可运行代码示例。
4. 「常见坑」板块（从 C 过来最易踩的）。
5. 「高频面试点」板块（面试常问 + 工作常用）。
6. 结尾「编译提醒」：`cl /EHsc /std:c++17 /W4 文件名.cpp`（多文件就列全）。
7. 与相邻模块呼应（前置知识回顾、后续伏笔）。

语气要求：知识型但不说教，简洁、务实、像同行搭档。用「你」称呼学习者。多用对比表、代码块。中文正文用纯文本，代码用 ```cpp 代码块。

答案代码要求：
- 每个 .cpp 顶部写编译命令注释。
- 关键行中文注释，解释「为什么这么写」而非复述语法。
- 必须能用 C++17 编译通过。

验证方式（重要）：本机 bash 沙箱里有 `g++`（ucrt64）。用 `g++ -std=c++17 -Wall -Wextra -fsyntax-only 文件.cpp` 做语法检查，用 `-c` 编译成目标文件验证。**注意：这个沙箱无法链接生成 .exe（链接器 exit 116，产出 0 字节），所以不要尝试运行 exe，只做 -fsyntax-only 和 -c 验证即可。** 多文件用分别 `-c` 各源文件验证。
